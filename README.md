🌦️ Weather App (React Native)

A simple React Native app to add cities and fetch real-time weather data using the OpenWeather API.

🚀 Tech Stack

React Native (CLI)

React Navigation

OpenWeather API

▶️ Run the App
npm install
cd ios && pod install && cd ..
npx react-native run-android
# or
npx react-native run-ios

🔑 API Key

Uses OpenWeather API

Store key in .env (do not commit)

Access via react-native-config

📄 Notes

package-lock.json and Podfile.lock are committed

.env is gitignored

Built using Flexbox layout

📜 License

MIT

If you want it even smaller (one-liner style) or interview-ready, tell me 👍

ChatGPT can make mistakes. Check important info.
